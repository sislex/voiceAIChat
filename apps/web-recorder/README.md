# @voicechat/web-recorder compatibility adapter

Implementation, tests and releases belong to [@sislexa/web-reader](https://github.com/sislex/webreader). This workspace preserves existing host imports and delegates checks to the pinned archive. Change implementation in that repository, release it, then update the archive and lockfile here.
