# @voicechat/playwright-reader-app compatibility adapter

Implementation, tests and releases belong to [@sislexa/playwright-reader](https://github.com/sislex/playwrightreader). This workspace preserves existing host imports and delegates checks to the pinned archive. Change implementation in that repository, release it, then update the archive and lockfile here.
