import { useSyncExternalStore } from 'react'
import { SplitChatWorkspace } from '@voicechat/chat-app'
import type { ReaderChatPort } from './contracts'
import type { WebReaderStore } from './store'
export function WebReaderApp({store,chat}:{store:WebReaderStore;chat:ReaderChatPort}):JSX.Element{const state=useSyncExternalStore(store.subscribe,store.getState,store.getState);return <main className="web-reader-app"><header><a href="#/">← В приложение</a><strong>Web Reader</strong><div role="tablist" aria-label="Панель"><button role="tab" aria-selected={state.mobilePane==='chat'} onClick={()=>store.setMobilePane('chat')}>Чат</button><button role="tab" aria-selected={state.mobilePane==='browser'} onClick={()=>store.setMobilePane('browser')}>Сайт</button></div></header>{state.error&&<p role="alert">{state.error}</p>}<SplitChatWorkspace mobilePane={state.mobilePane==='browser'?'right':'chat'} chat={chat.render(state.activeId)} rightPane={<section className="web-reader-browser" aria-label="Web Reader browser">{state.previewUrl?<iframe title="Предпросмотр сайта" src={'/api/preview?url='+encodeURIComponent(state.previewUrl)}/>:<p>Укажите адрес превью</p>}</section>}/></main>}
