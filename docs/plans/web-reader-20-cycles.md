# Web Reader: 20 циклов проверки и улучшений

Ветка: `codex/web-reader-20-cycles`. Ветка создана: 2026-09-10 01:27:38 +03:00 (git reflog).
Запрошено 20 циклов по 10 подтверждённых исправлений/улучшений, каждый с тестами,
проверкой в Chromium, актуализацией KB и отдельным коммитом. Коммиты разрешены
пользователем; push не запрошен. Работа автономная, без изменения данных прода.

## Учёт

Время фиксируется по часам машины. Для связанных исправлений указывается общее
окно реализации: это фактическое время группы, а не выдуманные независимые замеры.
Отдельно фиксируются исследование, реализация и проверки. Пункт считается готовым
только с доказательством проверки; запланированное не считается реализованным.

## Цикл 01 — HTML и загрузка ресурсов

Статус: готов, проверки зелёные. Исследование после создания ветки: 01:27:38–01:30:03;
реализация и unit/Chromium: 01:30:03–01:34:45 (4 мин 42 с на общую группу из 10 пунктов).
Первичная проверка git/KB до создания ветки отдельно не измерялась.
Проверки: первый `gate:fast` 01:32–01:37:26 — код 1 из-за устаревшего теста админки;
ремонт теста и его 43 проверки — 01:37:26–01:38:11; повторный `gate:fast`
01:38:11–01:44:34 — код 0; `gate` 01:44:34–01:50:14 — код 0 (typecheck,
все workspace-тесты, web build, Storybook build). Chromium 10/10, screenshot просмотрен.
Дополнительно: 01:49–01:50 реальный Web Reader на временной БД открыл страницу
входа нашего приложения по адресу проекта с `#/machines`. Сборка recorder — код 0.
Общее измеренное время цикла до коммита: 22 мин 36 с; основная часть — два широких
гейта и один полный, включая устранение исходного падения вне Reader.

| № | Подтверждённая проблема | Проверка |
|---|---|---|
| 01 | HTML entities в URL превращались в буквальный `amp;` у апстрима | unit + Chromium |
| 02 | URL-атрибуты без кавычек оставались на origin ChatAI | unit + Chromium |
| 03 | Строки HTML внутри script/comment переписывались как настоящие элементы | unit + Chromium |
| 04 | `data-src` и `data-href` ошибочно изменялись как `src`/`href` | unit + Chromium |
| 05 | `<base href>` не использовался для разрешения относительных ресурсов | unit + Chromium |
| 06 | Якоря `#…` превращались в загрузку нового документа | unit + Chromium |
| 07 | `formaction` кнопки отправлял форму мимо прокси | unit + Chromium |
| 08 | `target=_blank` без кавычек открывал внешнюю вкладку | unit + Chromium |
| 09 | SRI блокировал CSS/модули после изменения их содержимого прокси | unit + Chromium |
| 10 | Импорты inline module оставались вне прокси | unit + Chromium |

## Направления следующих циклов

Порядок уточняется по результатам проверки кода и браузера: CSS и модули;
SPA-навигация; формы; cookie; кэш и доступ к машинам; загрузка/ошибки;
адресная строка; lifecycle host; последовательные сценарии; сохранение сценариев;
поиск элементов; ввод и клавиатура; доступность; диагностика; сетевой контекст;
инспектор; мобильная панель; интеграция страниц проекта; внешние ресурсы.
Каждый цикл требует десяти конкретных пунктов и отдельного результата гейта.

## Среда проверки

Встроенный браузер Codex: подключение 10 сентября отклоняется инфраструктурной
ошибкой `missing field sandboxPolicy` до выполнения команды. Применяется реальный
Chromium из существующего e2e-стека проекта, с временными портами и данными.

### Сопутствующее восстановление гейта

01:37:26 быстрый гейт завершился кодом 1: `UsersAdmin.dom.test.tsx` ожидал
старые 7 колонок таблицы тарифов, текущий компонент имеет 8. Ожидания
обновлены по существующему компоненту, включая проверку режима и четырёх цен;
производственный UI не менялся. Это не засчитывается в 10 улучшений Reader.

## Цикл 02 — ES-модули и ресурсы сборки

Статус: готов. `gate:fast` 01:51:56–01:58:39 — код 0; `gate`
01:58:39–02:04:27 (время проверки завершения) — код 0. Все workspace-тесты,
web и Storybook собраны. Коммит цикла 01: `6d90d29b`.
Подготовка вне ветки во время гейта 01:39:57–01:42:58; подключение и проверки
01:50:45–01:51:56. Время группы: около 4 мин 12 с активной реализации/проверок;
гейт учитывается отдельно.

| № | Исправление | Проверка |
|---|---|---|
| 01 | Комментарии с import/from не переписываются | unit + Chromium |
| 02 | Строки с примерами импортов сохраняют содержимое | unit + Chromium |
| 03 | Текст template literal сохраняется, реальные выражения разбираются | unit + Chromium |
| 04 | Комментарии внутри настоящего import не мешают загрузке | unit + Chromium |
| 05 | Unicode escapes в спецификаторе декодируются | unit + Chromium |
| 06 | Статический template-адрес динамического import поддерживается | unit + Chromium |
| 07 | Абсолютные HTTP/HTTPS импорты и реэкспорты идут через прокси | unit + Chromium |
| 08 | JS публичных сайтов тоже переписывается, без требования алиаса | unit, общий путь ESM в Chromium |
| 09 | Явные имена и адреса import map согласованы с прокси | unit + Chromium |
| 10 | `new URL('./ресурс', import.meta.url)` разрешается от исходного модуля | unit + Chromium |

Адресно: 38/38 unit (модули, HTML, машины), 20/20 Chromium двух циклов.
`typecheck` server — код 0. Парсер JS — Acorn; неподдержанный синтаксис остаётся
исходным, чтобы показать естественную ошибку браузера, а не повредить код regexp.

### Находка при проверке настоящего приложения

01:52:58: логин внутри вложенного сайта остановился на `csrf`. Это отдельно
воспроизведённый исходный дефект: `authenticate` выбирает `vc_session` раньше
специальной Strict preview-cookie на защищённом POST `/api/preview`; форма логина
вложенного приложения закономерно не знает CSRF внешней оболочки. Исправление и
регрессии приоритета cookie назначены следующему циклу; вход пока не считается
проверенным успешно. Снимок: `/tmp/web-reader-artifacts/actual-reader-login-cycle02.png`.

## Цикл 03 — вход и данные авторизованных страниц

Статус: готов. `gate:fast` 02:06:32–02:11:12 — код 0; полный `gate`
02:11:12–02:21:02 (завершение проверено) — код 0. Всего 49 адресных unit
с дополнительной регрессией регистра заголовка; Chromium 11/11.
Подготовка тестов и кэша вне ветки во время предыдущего гейта: 01:45–02:02:50
(в этот интервал входили исследование CSRF и ожидания; это не чистое время кодинга).
Подключение исправлений, 48 unit и 11 Chromium-проверок: 02:05:15–02:06:32.
Первый typecheck нашёл неиспользуемое старое объявление кэша; оно удалено.

| № | Исправление | Проверка |
|---|---|---|
| 01 | На маршрутах превью Strict preview-cookie не перекрывается cookie внешней оболочки; остальной API требует CSRF | 6 auth-тестов + вход в настоящий App |
| 02 | Доступ и online машины проверяются до cache hit/304 | HTTP + Chromium |
| 03 | Ключ кэша включает пользователя | HTTP + Chromium |
| 04 | Браузер перепроверяет права при повторном чтении ресурса | HTTP + Chromium |
| 05 | JSON API исключён из минутного кэша статики | HTTP + Chromium |
| 06 | Cache-Control/private/no-store, Vary и Set-Cookie запрещают кэширование; no-store сохраняется при любом регистре заголовка | unit + HTTP + Chromium |
| 07 | Authorization и cookie сайта обходят публичные записи | HTTP + Chromium |
| 08 | no-cache/Pragma/Range требуют свежего ответа | unit + HTTP + Chromium |
| 09 | Мутации сбрасывают прежнюю статику машины | HTTP + Chromium |
| 10 | Кэш принадлежит экземпляру Reader, не смешивает мосты разных серверов | HTTP, общий кэш-путь в Chromium |

Подтверждён результат основного сценария: тестовый admin вошёл в собственное
приложение **внутри Web Reader**, открылось `#/machines`; снимок
`/tmp/web-reader-artifacts/reader-project-login.png` просмотрен. Данные и сервер теста удалены.
Коммит цикла 02: `1a5c4422`.

## Цикл 04 — навигация и живое состояние страницы

Статус: готов. `gate:fast` 02:23:35–02:25:57 — код 0; полный `gate`
02:25:57–02:31:59 (проверка завершения) — код 0. Подготовка вне ветки во время
гейта 03; её точный интервал отдельно не фиксировался. Применение и адресные проверки:
02:21:40–02:23 (около 1 мин 20 с); это не полное время подготовки группы.
87 unit/DOM, 11 Chromium, сборки recorder/web — код 0. Снимок перехода из машин
в чат просмотрен. Коммит цикла 03: `f4474be3`.

| № | Исправление | Проверка |
|---|---|---|
| 01 | Конечный URL после redirect становится runtime-базой | Chromium |
| 02 | Deep link восстанавливается без лишнего шага истории | Chromium |
| 03 | pushState сохраняет настоящий hash | Chromium |
| 04 | replaceState сохраняет hash и длину истории | Chromium |
| 05 | read/selected возвращают текущий, включая очищенный, hash | Chromium |
| 06 | SPA push/replace/hash/popstate сообщают готовый адрес host-у | Chromium |
| 07 | Адрес Recorder обновляется без перезагрузки и потери введённого draft | DOM |
| 08 | Сохранение URL разговора не перезагружает страницу; open возвращает конечный URL | DOM + настоящий App в Chromium |
| 09 | BFCache pageshow восстанавливает обработчик команд | Chromium |
| 10 | Запись, инспектор, редактирование и захват восстанавливаются при ready | DOM |

## Цикл 05 — формы и динамические ссылки

Статус: готов. Повторный `gate:fast` 02:37:32–02:46:35 — код 0; полный
`gate` 02:46:35–02:52:46 (проверка завершения) — код 0. Подготовка вне дерева
02:28:26–02:31:45 (с чтением KB и ожиданиями); применение и адресные проверки
02:32:19–02:32:46. 10 unit и 10 Chromium — код 0. Первый unit-прогон 9/10:
JSDOM не реализует IDL formMethod, обработчик читает атрибут как в HTML.
Коммит цикла 04: `e139188d`.

| № | Исправление | Проверка |
|---|---|---|
| 01 | GET-форма не теряет url прокси при сериализации query | Chromium |
| 02 | GET без action отправляется на реальный адрес документа | Chromium |
| 03 | GET сохраняет повторяющиеся поля, checkbox и submitter | Chromium |
| 04 | formaction/formmethod кнопки переопределяют маршрут и метод | unit + Chromium |
| 05 | Динамический POST остаётся в Reader с исходным телом | Chromium |
| 06 | Динамический multipart POST сохраняет файл и boundary | Chromium |
| 07 | Прямой form.submit() оборачивает GET | Chromium |
| 08 | Перехват форм уважает отмену приложения и dialog | unit + Chromium |
| 09 | Динамическая относительная ссылка остаётся внутри прокси | unit + Chromium |
| 10 | Ссылка из Shadow DOM находится по composedPath | Chromium |

Проверка типов цикла 05 впервые завершилась кодом 2: прямой импорт JSDOM требовал
`@types/jsdom`. Добавлена dev-зависимость server, отдельный typecheck зелёный.
Повторный `gate:fast` использует полный fallback из-за lockfile; после него идёт
обязательный `npm run gate`. Это время проверки группы, не время конкретного фикса.

## Цикл 06 — CSS и адаптивные изображения

Статус: готов. Повторный `gate:fast` — код 0; полный `gate`
03:06:54–03:14:00 (проверка завершения) — код 0. Первый прогон начат 02:53:49.
Подготовка вне дерева 02:33–02:36 (параллельно проверкам 05); пять parser assertions
проверены отдельно. Применение и адресные проверки 02:53:08–02:53:16.
21 unit (включая HTML-регрессии), 10 Chromium — код 0. Коммит цикла 05: `369ab366`.

| № | Исправление | Проверка |
|---|---|---|
| 01 | Quoted CSS URL со скобкой обрабатывается целиком | unit + Chromium |
| 02 | CSS hex escapes декодируются в настоящее имя ресурса | unit + Chromium |
| 03 | Комментарии с url() не изменяются | unit + Chromium |
| 04 | Текст content со строкой url() сохраняется | unit + computed style |
| 05 | Строковый @import загружается с media/layer | unit + computed style |
| 06 | Строковые источники image-set загружаются, type/плотность сохраняются | unit + Chromium |
| 07 | url(#SVG-filter) остаётся локальным фрагментом | unit + Chromium |
| 08 | Data URL внутри srcset остаётся декодируемой картинкой | unit + naturalWidth |
| 09 | Запятая в имени файла srcset сохраняется | unit + Chromium |
| 10 | imagesrcset preload обрабатывается так же, как picture/source | unit + Chromium |

Первый `gate:fast` цикла 06 завершился кодом 1 (проверено 03:01:10): старый
`rest.files.test.ts` ожидал CSS URL без кавычек. Теперь парсер сериализует URL в
кавычках, а HTML-атрибут экранирует их как `&quot;`; ожидание обновлено.
Адресный повтор: 36/36 unit, код 0. Повторный `gate:fast`
03:01:50–03:06:54 — код 0; полный `gate` стартовал после него.

## Цикл 07 — очередь модели и регистрации

Статус: готов. `gate:fast` 03:14:52.757–03:15:27.674 (34,917 с) — код 0;
полный `gate` 03:15:27.674–03:22:30.892 (423,218 с) — код 0.
Подготовка host-кода и тестов вне ветки: 02:38:34–02:42:25; unit-прогон 02:46,
Chromium-подготовка и проверки 02:57:13–02:58:08. При проверке подготовки цикла 08
уточнены dispose и промежуточный empty (02:52:25 и 03:03:12; отдельный замер
длительности этих уточнений не вёлся). Применение и адресные проверки в ветке:
03:14:21–03:14:28. 48 unit/DOM и 10 Chromium — код 0. Коммит 06: `e1a9c40c`.

| № | Исправление | Проверка |
|---|---|---|
| 01 | Viewport работает без готового документа: empty/loading/error | unit + Chromium |
| 02 | Новый open отменяет предыдущий и его DOM-очередь | unit + Chromium |
| 03 | Отложенный open не оживляет закрытый или заменённый iframe | unit + Chromium |
| 04 | Очистка страницы сразу завершает pending | unit + Chromium |
| 05 | Новый внешний/ручной адрес не получает click по старому документу | unit + Chromium |
| 06 | Старый handle регистрации больше не исполняет команды/диагностику | unit + Chromium |
| 07 | Ready чужого разговора не заменяет регистрацию | unit + Chromium |
| 08 | Ошибка postMessage возвращается модели и очищает таймеры | unit + Chromium |
| 09 | Disposed от shell закрывает host-регистрацию и очередь | unit + Chromium |
| 10 | Host-режимы инспектора и записи восстанавливаются после reload | unit + Chromium |

## Цикл 08 — модель в настоящем приложении

Статус: готов. Финальный fast 03:48:53.094–03:51:13.411 (140,317 с),
полный gate 03:51:13.412–03:57:41.683 (388,271 с) — оба код 0.
Повторные 11 Chromium-проверок на пересобранном web: 03:50:21–03:50:31 — код 0.
Первый fast (03:25:28–03:26:02, 34,555 с) выявил старое ожидание сохранения до
ready в App DOM; следующие (03:32:49–03:35:22, 153,626 с;
03:36:26–03:38:33, 127,446 с) — неполные типизированные фикстуры.
Полный typecheck всех пакетов затем прошёл. Fast 03:40:50–03:42:57
(126,651 с) прошёл; полный gate 03:42:57–03:47:24 (266,593 с) обнаружил
статический импорт из ленивого Reader-пакета. Помощники перенесены в слой UI;
111 тестов App/ленивых экранов/помощников прошли 03:48:20–03:48:30.
Подготовка вне дерева 02:46–03:06:40 (код, unit, настройка стенда и Chromium;
это общее окно группы, не чистое время кодинга). Применение и проверки в ветке
03:24:06–03:24:55. 61 unit/DOM + 33 relay/MCP, UI typecheck, web build,
11 Chromium — код 0. Коммит 07: `4be6ddac`.

| № | Исправление | Проверка |
|---|---|---|
| 01 | reader.changed после навигации не перемонтирует живой документ | Chromium: один запрос вместо двух |
| 02 | Open модели имеет единственный путь навигации, без предварительного обновления props | unit + Chromium |
| 03 | Cookie-гейт модели работает даже из пустого Reader, отказ виден и повторяем | DOM + Chromium |
| 04 | Сохраняется подтверждённый конечный URL; ошибка загрузки не сохраняет плохой адрес | DOM + redirect в Chromium |
| 05 | Ошибка сохранения показана с повтором без reload | DOM + Chromium |
| 06 | Сохранения сериализованы и используют callback исходного разговора | DOM |
| 07 | Поздний результат модели несёт ID начавшей его регистрации | unit |
| 08 | Исключение runner превращается в ответ модели | unit |
| 09 | Устаревшая/неуспешная диагностика не меняет ошибки текущей страницы | unit |
| 10 | reader.changed получает адрес и заголовок из вложенного result.page | relay unit + Chromium |

Исходный baseline после корректной настройки стенда подтвердил две загрузки на
один model open. Первые отказы стенда (алиас вне process.env, GET без scope)
исправлены в тестах и не входят в десять дефектов продукта.

## Цикл 09 — адресная строка и загрузка страницы

Статус: 51 unit/DOM, typecheck и build — код 0; 31 Chromium
03:58:57–03:59:24 — код 0. Коммит 08: `ab93e3a5`.
Первый fast 04:00:06.402–04:00:49.659 (43,257 с) остановился на стороже
графа workspace-зависимостей: добавлена прямая связь web-recorder → ui-kit.
Повторные гейты зелёные: fast 04:01:55.474–04:08:03.436 (367,962 с),
полный gate 04:08:03.437–04:15:07.511 (424,075 с), код 0.
Подготовка шла вне дерева одновременно с гейтами 08; отдельный начальный замер
кодинга не фиксировался. Подтверждённые окна: Chromium 03:33:56–03:34:03;
исправление гонки onLoad, найденной сценарием цикла 10, 03:41:38–03:42:47;
финальные подготовительные DOM-проверки 03:54:11–03:54:15.
Применение в ветке — 03:58; unit/typecheck/build — с 03:58:34.
Это общие окна группы из десяти связанных правок, не отдельные независимые замеры.

| № | Исправление | Проверка |
|---|---|---|
| 01 | Стандартный HTML-документ, язык и мобильный viewport | Chromium |
| 02 | Собственный reset, шрифты, геометрия и кнопки UI kit | Chromium + build |
| 03 | Адрес без схемы: публичный HTTPS и локальное окружение HTTP | unit + Chromium |
| 04 | Относительный путь/query/hash от актуального URL | unit + Chromium |
| 05 | Загрузка, понятный отказ и повтор; старые callbacks не портят новый переход | DOM + Chromium |
| 06 | Обновление текущей страницы после SPA-навигации | Chromium |
| 07 | Взаимоисключение инспектора, редактора и захвата | DOM + Chromium |
| 08 | Закрытие инструментов снаружи/Escape и восстановление фокуса | DOM + Chromium |
| 09 | Настоящая заданная ширина viewport внутри узкой панели | Chromium |
| 10 | Тулбар на узком экране, удобные размеры и aria-состояние записи | Chromium |

## Цикл 10 — последовательное воспроизведение

Статус: готов. 69 unit/DOM, typecheck и Recorder build — код 0;
31 Chromium 04:17:10–04:17:28 — код 0. Fast
04:18:02.752–04:18:05.600 (2,848 с), полный gate
04:18:05.600–04:23:54.684 (349,084 с) — оба код 0.
Коммит 09: `2f4db9fa`. Подготовка runner/UI/tests вне дерева — 03:34–03:38;
10 Chromium и 68 подготовительных unit/DOM прошли после уточнения гонки onLoad
03:42:42–03:42:47. Typecheck уточнён после удаления неиспользуемого импорта.
Вёрстка прогресса уточнена после просмотра снимка в 03:48. Применение в ветке:
04:15:49; адресные проверки с 04:15:57. Время указано для связанной группы правок.

| № | Исправление | Проверка |
|---|---|---|
| 01 | Следующий шаг ждёт коррелированный ответ предыдущего | unit + Chromium |
| 02 | Переход ожидает готовность нового документа | unit + Chromium |
| 03 | Первая ошибка останавливает оставшиеся действия | unit + Chromium |
| 04 | Кнопка остановки отменяет ожидания и поздние ответы | unit + Chromium |
| 05 | Open/reload/dispose отменяют старый запуск | DOM + Chromium |
| 06 | Все поля/секреты проверяются заранее, временные секреты очищаются | unit + DOM + Chromium |
| 07 | Таймауты шага и страницы ограничены, таймеры очищаются | unit |
| 08 | Видны статус, выполненные шаги и причина остановки | DOM + Chromium |
| 09 | Нет параллельного запуска, редактирования и смешивания с командами модели | unit + DOM + Chromium |
| 10 | Сценарий использует снимок списка и не записывает собственное воспроизведение | unit + Chromium |

## Цикл 11 — сохранение, редактирование и экспорт сценариев

Статус: готов. Коммит 10: `0a0f4423`.
31 Chromium 04:25:35–04:26:15 — код 0. Fast
04:26:54.182–04:32:00.125 (305,943 с), полный gate
04:32:00.126–04:41:30.686 (570,560 с) — оба код 0.
Подготовка вне дерева 03:43–03:54; уточнение SPA-сценариев и проверки
04:18–04:19:18. 93 подготовительных unit/DOM и 10 Chromium — код 0.
Применение 04:24:20; 83 Recorder + 10 shared тестов, typecheck/build — код 0
с 04:24:33. Лимиты полей редактора уточнены в 04:25, сборка повторена.
Это окна работы над общей группой; ошибки временных путей стенда, title кнопок
и типа DOM-test options устранены до применения и не считаются дефектами продукта.

| № | Исправление | Проверка |
|---|---|---|
| 01 | Повреждённые/null/неверно типизированные шаги не ломают список | shared + DOM + Chromium |
| 02 | Ограничены JSON, число шагов и размеры полей | shared + Chromium |
| 03 | Секреты старых записей маскируются; успешный перенос удаляет legacy | shared + storage + Chromium |
| 04 | Выключенная запись игнорирует сообщения, включение не теряет быстрый ввод | DOM + Chromium |
| 05 | Посимвольный ввод склеивается до click/submit | shared + Chromium |
| 06 | Полный URL и SPA-переходы выбирают отдельные сценарии страниц | storage + DOM + Chromium |
| 07 | Ручная отметка секрета стирает обычное значение | DOM + Chromium |
| 08 | Удаление/порядок шагов не переносят временные секреты между полями | DOM + Chromium |
| 09 | Экспорт многострочного текста остаётся исполняемым JavaScript | unit + Chromium/download |
| 10 | Экспорт проверяет секреты до навигации и первого действия | unit + Chromium/download |

## Цикл 12 — cookies и вход на сайты

Статус: готов. Коммит 11: `ad19ca64`.
34 unit/integration и typecheck — код 0; 11 Chromium
04:42:52–04:43:05 — код 0. Fast 04:43:40.075–04:49:21.273 (341,198 с),
полный gate 04:49:21.273–04:55:51.530 (390,257 с) — оба код 0.
Подготовка вне дерева 03:55–04:07: 30 unit/integration и 10 Chromium — код 0.
Применение в ветке 04:42; дополнительно найден и исправлен сброс MCP через старый
глобальный контейнер, добавлена проверка связи модуля. Окна времени общие для группы.

| № | Исправление | Проверка |
|---|---|---|
| 01 | Host-only cookie не передаётся поддоменам | unit + Chromium |
| 02 | Path проверяется по границе сегмента | unit + Chromium |
| 03 | Default-path сохраняет родительский путь без лишнего slash | unit + Chromium |
| 04 | Max-Age имеет приоритет независимо от порядка атрибутов | unit + Chromium |
| 05 | Public suffix и общий Domain машин не объединяют сессии | unit + Chromium |
| 06 | Secure и __Host-/__Secure- проверяются при установке и отправке | unit + Chromium |
| 07 | Expires epoch и Max-Age=0 удаляют сессию | unit + Chromium |
| 08 | Изоляция экземпляров, закрытие и общий HTTP/MCP reset | unit/integration |
| 09 | Регистр Set-Cookie и повреждённые отдельные значения не теряют соседние | unit + Chromium |
| 10 | Cookie промежуточного redirect попадает в следующий запрос | Chromium |
