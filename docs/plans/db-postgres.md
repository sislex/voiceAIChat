# Postgres как движок базы — план перевода слоя данных

Живая реализация: `apps/server/src/db/sql/` (адаптер `Sql` над better-sqlite3 и node-postgres),
`apps/server/src/db/repos/<домен>.ts` (асинхронные репозитории, один код на оба движка),
`apps/server/src/db/schemaPg.ts` (DDL Postgres, выводится из SQLite-схемы и проверяется тестом),
`scripts/db-to-postgres.ts` (перенос данных).

## Зачем

Цель — запускать канбан, машины, админку и прочие части приложения на любом сервере, оставив
центральными чат, базу и исполнителей LLM (`docs/plans/make-standalone.md` сделал это для Make —
у него нет таблиц). Всё остальное таблицы имеет, и **SQLite к этому не годится**: это файл, его
нельзя открыть с другого хоста. Значит, «указать в настройках, где лежит база» возможно только
для сетевой базы — Postgres.

Разрез слоя данных (`docs/plans/db-repositories.md`) подготовил почву: владельцы таблиц,
асинхронные порты, PoC `releases` на pglite за тем же портом. Но PoC пошёл путём «вторая реализация
домена на другом движке» — для 6,5 тыс. строк остальных доменов это переписывание с высоким риском
расхождений. Здесь путь другой: **один код репозитория на оба движка** над узким адаптером `Sql`,
а различия диалектов закрываются в одном месте — транслятором SQL и DDL для Postgres.

## Решения

- **Вся база на одном движке**, а не домен за доменом. Кросс-доменные JOIN'ы (`CROSS_READ_BUDGET`)
  остаются работать; сервисы, которые уедут на другие хосты, подключаются к тому же Postgres и
  работают со своими таблицами тем же кодом репозиториев. Переключатель — `VC_DB_URL`
  (`postgres://…`); без него — SQLite, как сейчас. PoC pglite/`VC_DB_RELEASES` снимается.
- **Репозитории становятся асинхронными внутри**: `await this.sql.get/all/run(sql, params)`
  вместо `this.db.prepare(sql).get(...)`. Для SQLite это обёртка над тем же синхронным драйвером
  (сервер и так говорит с данными через async-порты), для Postgres — настоящий await.
- **Транзакции — `await this.sql.transaction(async () => …)`** с AsyncLocalStorage: всё, что
  выполняется внутри колбэка (включая вызовы соседей `this.repos.*`), идёт через соединение
  транзакции; вложенные — через SAVEPOINT. У SQLite одно соединение, поэтому на время открытой
  транзакции остальные запросы ждут (мьютекс) — иначе чужой запрос попал бы внутрь чужой транзакции.
- **Постгресовский DDL выводится из SQLite-схемы** программно (`INTEGER`→`BIGINT` для меток
  времени и счётчиков, `REAL`→`DOUBLE PRECISION`, `AUTOINCREMENT`→`BIGSERIAL`, у каждой таблицы
  явный `rowid BIGSERIAL` — код, которому нужен порядок вставки, не меняется), результат
  зафиксирован в `schemaPg.ts` и сверяется тестом с `schema.ts`, чтобы они не разъехались.
- **Диалект в одном транслятере** (`db/sql/dialect.ts`): `?`→`$n`, `INSERT OR IGNORE`→
  `ON CONFLICT DO NOTHING`, `IFNULL`→`COALESCE`, `MAX(a, b)`/`MIN(a, b)`→`GREATEST`/`LEAST`,
  `LIKE`→`ILIKE` (у SQLite LIKE регистронезависим), `ulower()`→`lower()`, `json_extract(x, '$.k')`→
  `(x::json->>'k')`, `IS ?`→`IS NOT DISTINCT FROM $n`. Что транслятор не берёт — правится в репозитории
  так, чтобы работало на обоих движках. Полнотекстовый поиск сообщений (FTS5) на Postgres — `tsvector`.
- **Миграции существующих SQLite-баз (`migrate()`, 186 ALTER TABLE) остаются SQLite-only**:
  Postgres-база создаётся переносом из SQLite уже в актуальной схеме, поэтому ей нужны только
  будущие миграции — они пишутся сразу для обоих движков.
- **Проверка — тем же набором тестов.** `VC_TEST_DB_URL=postgres://…` заставляет
  `new VoiceChatDb(':memory:')` в тестах открывать свежую схему в Postgres: вся матрица тестов
  сервера (2,2 тыс.) гоняется на втором движке без переписывания.

## Круги

### Круг 1 — асинхронный адаптер и репозитории на нём ☑ (2026-09-07)
1. ☑ `db/sql/types.ts` (`Sql`: `get/all/run/exec/transaction/prepare`), `db/sql/sqlite.ts`
   (better-sqlite3 + ALS-транзакции + мьютекс), тесты адаптера (вложенность, ожидание чужой транзакции).
2. ☑ Codemod репозиториев (`scratchpad/sqlify.mjs`, фикспойнт асинхронности по классам: 567 из 585 методов; 14 async-колбэков в forEach/filter/find переписаны циклами руками; аудит `promise-audit.mjs` — ни одного промиса вне await) и `database.ts`: `prepare().get/all/run` → `await this.sql.*`,
   `this.db.transaction(() => …)()` → `await this.sql.transaction(async () => …)`, методы → `async`,
   вызовы соседей → `await`; `RepoContext.db` → `RepoContext.sql`.
3. ☑ `VoiceChatDb`: схема и миграции асинхронно (`ready`), порты ждут `ready`; `db.sync` → `db.impl`
   (алиас `sync` сохраняется для тестов). PoC pglite удалён.
4. ☑ Гейт зелёный на SQLite. Не из плана: **полоса** `db/sql/lane.ts` — методы через порты по одному, в порядке вызова; без неё параллельные запросы перемежались бы внутри многошаговых методов (регрессия против синхронного драйвера), а десятки тестов, зовущих порты без `await`, ловили гонки.

### Круг 2 — Postgres ☑ (2026-09-07)
1. ☑ `db/sql/pg.ts` (node-postgres, int8/numeric → Number, пул, транзакции на выделенном клиенте через AsyncLocalStorage,
   `VC_SQL_TRACE=1` — трассировка запросов), `db/sql/dialect.ts` с тестом на каждое правило. Правил оказалось больше плана:
   camelCase-алиасы в кавычки (Postgres опускает регистр), явные `::bigint`/`::double precision` у числовых
   параметров (иначе `CASE … THEN $n` — text), `? IS NULL` → литерал по значению, `LIMIT -1` → `LIMIT ALL`,
   `CASE WHEN $n` → `<> 0`, `substr(x, -N)` → `right(x, N)`, квалификация голых колонок в `DO UPDATE SET`.
2. ☑ `schemaPg.ts`: генератор из `schema.ts` (таблицы, индексы, сиды, FK отдельными `ALTER`, `rowid` у каждой
   таблицы) + тест-сверка с манифестом владения и применение к настоящему Postgres дважды; FTS сообщений —
   `text_tsv tsvector` + GIN, триггеры `cost_dirty` на plpgsql. Попутно: 30 колонок жили только в ALTER миграций и
   отсутствовали в `schema.ts` — дописаны, гейт `schemaPg.test.ts` теперь требует каждую ALTER-колонку в CREATE TABLE.
3. ☑ `VC_TEST_DB_URL`: вся матрица тестов сервера на Postgres в docker (`postgres:16-alpine -c max_locks_per_transaction=1024`).
   Найден и исправлен настоящий дефект: кадры WS, пришедшие пока идёт проверка сессии, терялись (слушатель появлялся
   только в `attachWs`); с SQLite окно было незаметно. Исключения на Postgres: тесты сырого драйвера/файловых баз
   (`ON_POSTGRES`) и **карантин из 26 тестов** менеджеров (очередь ходов, слоты ранов, relay превью, kb.usage,
   оркестрация планов) — они
   опираются на порядок событий синхронного драйвера; аудит параллелизма — отдельный пункт ниже.
4. ☑ `VC_DB_URL` в конфиге и `server.ts`. Полоса вызовов включена и на Postgres (`DbDeps.concurrent` снимает):
   сервер опирается на порядок вызовов и атомарность многошаговых методов внутри процесса. Резервных копий
   через `VACUUM INTO` в коде сервера нет — снимки прода делаются вручную (memory: prod-db-copy-check).

### Круг 3 — перенос данных и compose ☑ (2026-09-07)
1. ☑ `apps/server/src/db/copyToPostgres.ts` + CLI `copyToPostgres.cli.ts`: все таблицы, включая `rowid`, пакетами,
   FK-триггеры выключены на время копирования, последовательности выровнены, счётчики строк; тест на Postgres.
2. ☑ compose: сервис `postgres` (профиль `postgres`, `VC_PG_PASSWORD` в `.env`, `max_locks_per_transaction=1024`),
   `VC_DB_URL` у ядра; KB (`deploy.md`, `data-auth.md`, `conventions.md`, `docs/docker.md`), `apps/server/AGENTS.md`.
3. ☑ Прогон на копии прод-БД (4,9 ГБ, 113 таблиц): перенос за 107 с, счётчики совпали по всем таблицам
   (две строки `ci_run_logs` с NUL-байтами — Postgres их не хранит, байт вырезается при переносе); сервер на
   `VC_DB_URL` с прод-данными — 160 бесед со стоимостью, доска 14×24, карточка, 445 релизов, отчёт расхода
   (1 891 сообщение, 40 бакетов), поиск с курсором, админка, машины, Make, KB, команды CI — все 200.
4. ☑ PR.

## Долг после круга 2 — аудит параллелизма перед Postgres в проде

С SQLite тело метода репозитория выполнялось целиком при вызове, и код менеджеров (очередь ходов
`turns.ts`, слоты и отмена ранов `ci/runManager.ts`, relay превью, кадры kb.usage) неявно
опирался на это: check-then-act через два вызова портов, `setImmediate` как «БД уже ответила»,
проверка состояния в памяти сразу после записи. Полоса вернула порядок вызовов, но между
вызовами сервера остались окна, которые Postgres делает наблюдаемыми. Один такой дефект уже
пойман и исправлен (потеря ранних WS-кадров); 26 тестов помечены `it.skipIf(ON_POSTGRES)`. До
включения `VC_DB_URL` в проде: пройти карантин по одному, заменяя гонки на замки по ключу
(`startForDevelopmentTransition`, `turns.start` до `starting.add`) или транзакции, и снять пометки.

## Риски
- **Строгий `GROUP BY` Postgres** (у SQLite — нет): 19 запросов пересматриваются по одному.
- **Курсор поиска**: `ts_rank_cd` отдаёт float4 — ранг приводится к double до сравнения с курсором, иначе
  страницы теряют строки с равным рангом (поймано матрицей).
- **`rowid`**: явный `rowid BIGSERIAL` в DDL закрывает `ORDER BY rowid`/`WHERE rowid = ?`; вставка
  с явным rowid есть только у FTS-триггеров чата, которые на Postgres не нужны.
- **Мьютекс транзакций в SQLite**: если тело транзакции будет ждать внешнее событие, которое само
  ходит в базу из другого контекста, — взаимная блокировка. Тела репозиториев внешнего не ждут
  (они были синхронными); правило записывается в conventions.
- **Производительность**: на SQLite добавляется микрозадача на запрос — при 900 запросах в коде это
  незаметно; циклы с сотнями `run` остаются в одной транзакции.
