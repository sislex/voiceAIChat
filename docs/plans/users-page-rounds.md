# Страница «Пользователи» и «Мой аккаунт» — круги улучшений

Живая реализация: `packages/admin-app/src/users/*` (страница), `packages/profile-app`
(карточка человека, общая с `#/account`), `packages/ui-kit` (примитивы),
`apps/server/src/routes/{admin,rest}.ts` (данные). Макет — Make-публикация
`/p/cae6955712984a7dbde8ff4ef639f3b3/`.

Правило кругов: круг засчитывается только после зелёного `npm run gate` **по коду
возврата** и проверки глазами в браузере. Данные не выдумываем: нет источника —
честное пустое состояние либо доработка сервера.

## Круг 1 — страница есть, но она ещё дороже и грубее, чем должна быть

После первой сборки остались измеримые долги: список грузится целиком без предела,
поиск фильтрует на каждый символ, журнал тянется в двести событий ради пяти строк
обзора, а данные вкладки перезапрашиваются при каждом возврате на неё. Плюс мелочи
интерфейса, которые видно глазами, но не видно тестам.

| № | Пункт | Статус |
|---|---|---|
| 1 | Кэш данных вкладки по ключу «человек + вкладка + период»: возврат на вкладку не бьёт в сервер | ✅ |
| 2 | Обзору хватает 20 событий, журналу — 200: лимит по месту вызова, а не один на всё | ✅ |
| 3 | Дебаунс поиска 200 мс на ввод: список из сотен строк не пересобирается на каждый символ | ✅ |
| 4 | Виртуальный предел списка: показываем 200 строк и кнопку «показать ещё» | ✅ |
| 5 | `AdminUserInfo.machineCount` вместо полного списка машин в списке людей: ответ короче | ✅ |
| 6 | Отмена гонки при быстром переключении вкладок: побеждает последний ответ, а не последний пришедший | ✅ |
| 7 | Индекс `sessions(user_name, revoked_at, expires_at)` под агрегат активности | ✅ |
| 8 | `db.conversationCounts()` и `sessionActivity()` — один SQL-проход вместо двух запросов | ✅ |
| 9 | Байты в человеческом виде: ГБ/ТБ вместо «55798.6 МБ» на терабайтном разделе | ✅ |
| 10 | Компонент `Toolbar` в ui-kit: строка «N разрешено · M запрещено» + действия | ✅ |
| 11 | Компонент `DefinitionList` в ui-kit для пар «подпись → значение» | ✅ |
| 12 | Метрики на 390px в одну колонку, как в макете ниже 480px | ✅ |
| 13 | Сокращение User-Agent в журнале: «Chrome 141 · macOS» вместо строки на 120 символов | ✅ |
| 14 | Пустой поиск по моделям не прячет провайдера целиком, а показывает «ничего не найдено» | ✅ |
| 15 | Клавиша Esc в поиске очищает запрос | ✅ |
| 16 | Фокус переезжает на карточку при выборе человека с клавиатуры | ✅ |
| 17 | `aria-live` на счётчике списка: скринридер узнаёт результат фильтрации | ✅ |
| 18 | Скелетон карточки человека вместо пустоты, пока грузится расход | ✅ |
| 19 | Ошибка загрузки вкладки видна в самой вкладке, а не только тостом | ✅ |
| 20 | Сториз ui-kit и profile-app в тёмной теме: контраст бейджей и полос проверяем глазами | ✅ |
