# Круг 27: проход под developer чисто, витрина дополнена

Источник находок сменился на ручной проход интерфейса под ролью developer
(живой стенд, отдельный пользователь): роль-подсказка, недоступность правки
режима доступа, правка БЗ, тумблеры, добавление своей инструкции, сохранение
пресета, отмена из журнала, экспорт CSV — **дефектов не нашлось**. Один сбой
(клик по тумблеру без эффекта) не воспроизвёлся прицельным повтором — похоже,
моя автоматизация попала в момент `busy`; за баг не считаю и не чиню.

Сделано по делу:
- сториз `PresetPreviewFlow` и `JournalWithSettings` — новые состояния кругов
  13 и 15 не были представлены в витрине; оба с `play`, попадают под общий
  axe-прогон;
- пара контраста бейджа «тяжёлый» (`--text-dim` на `--surface-hover`) уже в
  `CONTRAST_PAIRS` — проверено, работы не потребовалось.

## Вывод

Раздел «Контекст и инструкции» выработан всеми четырьмя способами: идеи,
факты кода, аудит правок, ручной проход. Следующему кругу здесь делать нечего.
